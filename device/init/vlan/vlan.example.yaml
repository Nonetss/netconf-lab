# yaml-language-server: $schema=./vlan.schema.json
"ietf-interfaces:interfaces":
  interface:
  - name: null  # string
    description: null  # string
    type: null  # identityref base interface-type, mandatory
    enabled: true  # boolean
    link-up-down-trap-enable: null  # enumeration, if-feature if-mib, enum: enabled|disabled
"openconfig-acl:acl":
  config:
  acl-sets:
    acl-set:
    - name: null  # leafref
      type: null  # leafref
      config:
        name: null  # string
        type: null  # identityref base ACL_TYPE
        description: null  # string
      acl-entries:
        acl-entry:
        - sequence-id: null  # leafref
          config:
            sequence-id: null  # uint32
            description: null  # string
          l2:
            config:
              source-mac: null  # oc-yang:mac-address
              source-mac-mask: null  # oc-yang:mac-address
              destination-mac: null  # oc-yang:mac-address
              destination-mac-mask: null  # oc-yang:mac-address
              ethertype: null  # oc-pkt-match-types:ethertype-type
          ipv4:
            config:
              source-address: null  # oc-inet:ipv4-prefix
              source-address-prefix-set: null  # leafref
              destination-address: null  # oc-inet:ipv4-prefix
              destination-address-prefix-set: null  # leafref
              fragment-offsets: []  # leaf-list, oc-pkt-match-types:fragment-offset-range
              dscp: null  # oc-inet:dscp
              dscp-set: []  # leaf-list, oc-inet:dscp
              length: null  # uint16
              protocol: null  # oc-pkt-match-types:ip-protocol-type
              hop-limit: null  # uint8
            icmpv4:
              config:
                type: null  # identityref base oc-icmpv4-types:TYPE
                code: null  # identityref base oc-icmpv4-types:CODE
          mpls:
            config:
              traffic-class: null  # oc-mpls:mpls-tc
              start-label-value: null  # oc-mpls:mpls-label
              end-label-value: null  # oc-mpls:mpls-label
              ttl-value: null  # uint8
          ipv6:
            config:
              source-address: null  # oc-inet:ipv6-prefix
              source-address-prefix-set: null  # leafref
              source-flow-label: null  # oc-inet:ipv6-flow-label
              destination-address: null  # oc-inet:ipv6-prefix
              destination-address-prefix-set: null  # leafref
              destination-flow-label: null  # oc-inet:ipv6-flow-label
              flow-label: null  # oc-inet:ipv6-flow-label
              dscp: null  # oc-inet:dscp
              dscp-set: []  # leaf-list, oc-inet:dscp
              length: null  # uint16
              protocol: null  # oc-pkt-match-types:ip-protocol-type
              hop-limit: null  # uint8
            icmpv6:
              config:
                type: null  # identityref base oc-icmpv6-types:TYPE
                code: null  # identityref base oc-icmpv6-types:CODE
          transport:
            config:
              source-port: null  # oc-pkt-match-types:port-num-range
              source-port-set: null  # leafref
              destination-port: null  # oc-pkt-match-types:port-num-range
              destination-port-set: null  # leafref
              detail-mode: null  # enumeration, enum: EXPLICIT|BUILTIN
              explicit-detail-match-mode: null  # enumeration, enum: ANY|ALL
              explicit-tcp-flags: []  # leaf-list, identityref base oc-pkt-match-types:TCP_FLAGS
              builtin-detail: null  # enumeration, enum: TCP_INITIAL|TCP_ESTABLISHED|FRAGMENT
          input-interface:
            config:
            interface-ref:
              config:
                interface: null  # leafref
                subinterface: null  # leafref
          actions:
            config:
              forwarding-action: null  # identityref base FORWARDING_ACTION, mandatory
              log-action: LOG_NONE  # identityref base LOG_ACTION
  interfaces:
    interface:
    - id: null  # leafref
      config:
        id: null  # oc-if:interface-id
      interface-ref:
        config:
          interface: null  # leafref
          subinterface: null  # leafref
      ingress-acl-sets:
        ingress-acl-set:
        - set-name: null  # leafref
          type: null  # leafref
          config:
            set-name: null  # leafref
            type: null  # leafref
      egress-acl-sets:
        egress-acl-set:
        - set-name: null  # leafref
          type: null  # leafref
          config:
            set-name: null  # leafref
            type: null  # leafref
"openconfig-bfd:bfd":
  interfaces:
    interface:
    - id: null  # leafref
      config:
        id: null  # oc-if:interface-id
        enabled: null  # boolean
        local-address: null  # oc-inet:ip-address
        desired-minimum-tx-interval: null  # uint32
        required-minimum-receive: null  # uint32
        detection-multiplier: null  # uint8
        enable-per-member-link: false  # boolean
        desired-min-echo-tx-interval: null  # uint32
        required-min-echo-rx-interval: null  # uint32
      interface-ref:
        config:
          interface: null  # leafref
          subinterface: null  # leafref
      micro-bfd-sessions:
        micro-bfd-session:
        - member-interface: null  # leafref
          config:
            local-address: null  # oc-inet:ip-address
            remote-address: null  # oc-inet:ip-address
            member-interface: null  # leafref
      peers:
"openconfig-defined-sets:defined-sets":
  ipv4-prefix-sets:
    ipv4-prefix-set:
    - name: null  # leafref
      config:
        name: null  # string
        description: null  # string
        prefix: []  # leaf-list, oc-inet:ipv4-prefix
        exclude-prefix: []  # leaf-list, oc-inet:ipv4-prefix
  ipv6-prefix-sets:
    ipv6-prefix-set:
    - name: null  # leafref
      config:
        name: null  # string
        description: null  # string
        prefix: []  # leaf-list, oc-inet:ipv6-prefix
        exclude-prefix: []  # leaf-list, oc-inet:ipv6-prefix
  port-sets:
    port-set:
    - name: null  # leafref
      config:
        name: null  # string
        description: null  # string
        port: []  # leaf-list, oc-pkt-match-types:port-num-range
        exclude-port: []  # leaf-list, oc-pkt-match-types:port-num-range
"openconfig-interfaces:interfaces":
  interface:
  - name: null  # leafref
    config:
      name: null  # string
      type: null  # identityref base ietf-if:interface-type, mandatory
      mtu: null  # uint16
      loopback-mode: null  # oc-opt-types:loopback-mode-type
      description: null  # string
      enabled: true  # boolean
      "openconfig-vlan:tpid": oc-vlan-types:TPID_0X8100  # identityref base oc-vlan-types:TPID_TYPES
    hold-time:
      config:
        up: 0  # uint32
        down: 0  # uint32
    penalty-based-aied:
      config:
        max-suppress-time: 0  # uint32
        decay-half-life: 0  # uint32
        suppress-threshold: 0  # uint32
        reuse-threshold: 0  # uint32
        flap-penalty: 0  # uint32
    subinterfaces:
      subinterface:
      - index: null  # leafref
        config:
          index: 0  # uint32
          description: null  # string
          enabled: true  # boolean
        "openconfig-vlan:vlan":
          config:
            vlan-id: null  # union
          match:
            single-tagged:
              config:
                vlan-id: null  # oc-vlan-types:vlan-id
            single-tagged-list:
              config:
                vlan-ids: []  # leaf-list, oc-vlan-types:vlan-id
            single-tagged-range:
              config:
                low-vlan-id: null  # oc-vlan-types:vlan-id
                high-vlan-id: null  # oc-vlan-types:vlan-id
            double-tagged:
              config:
                inner-vlan-id: null  # oc-vlan-types:vlan-id
                outer-vlan-id: null  # oc-vlan-types:vlan-id
            double-tagged-inner-list:
              config:
                inner-vlan-ids: []  # leaf-list, oc-vlan-types:vlan-id
                outer-vlan-id: null  # oc-vlan-types:vlan-id
            double-tagged-outer-list:
              config:
                inner-vlan-id: null  # oc-vlan-types:vlan-id
                outer-vlan-ids: []  # leaf-list, oc-vlan-types:vlan-id
            double-tagged-inner-range:
              config:
                inner-low-vlan-id: null  # oc-vlan-types:vlan-id
                inner-high-vlan-id: null  # oc-vlan-types:vlan-id
                outer-vlan-id: []  # leaf-list, oc-vlan-types:vlan-id
            double-tagged-outer-range:
              config:
                inner-vlan-id: null  # oc-vlan-types:vlan-id
                outer-low-vlan-id: null  # oc-vlan-types:vlan-id
                outer-high-vlan-id: null  # oc-vlan-types:vlan-id
            double-tagged-inner-outer-range:
              config:
                inner-low-vlan-id: null  # oc-vlan-types:vlan-id
                inner-high-vlan-id: null  # oc-vlan-types:vlan-id
                outer-low-vlan-id: null  # oc-vlan-types:vlan-id
                outer-high-vlan-id: null  # oc-vlan-types:vlan-id
          ingress-mapping:
            config:
              vlan-stack-action: null  # oc-vlan-types:vlan-stack-action
              vlan-id: null  # oc-vlan-types:vlan-id
              tpid: null  # identityref base oc-vlan-types:TPID_TYPES
          egress-mapping:
            config:
              vlan-stack-action: null  # oc-vlan-types:vlan-stack-action
              vlan-id: null  # oc-vlan-types:vlan-id
              tpid: null  # identityref base oc-vlan-types:TPID_TYPES
    "openconfig-if-ethernet:ethernet":
      config:
        mac-address: null  # oc-yang:mac-address
        enable-flow-control: false  # boolean
        auto-negotiate: true  # boolean
        standalone-link-training: false  # boolean
        duplex-mode: null  # enumeration, enum: FULL|HALF
        port-speed: null  # identityref base ETHERNET_SPEED
        fec-mode: null  # identityref base INTERFACE_FEC
        "openconfig-if-aggregate:aggregate-id": null  # leafref
      "openconfig-vlan:switched-vlan":
        config:
          interface-mode: null  # oc-vlan-types:vlan-mode-type
          native-vlan: null  # oc-vlan-types:vlan-id
          access-vlan: null  # oc-vlan-types:vlan-id
          trunk-vlans: []  # leaf-list, union
    "openconfig-if-aggregate:aggregation":
      config:
        lag-type: null  # aggregation-type
        min-links: null  # uint16
      "openconfig-vlan:switched-vlan":
        config:
          interface-mode: null  # oc-vlan-types:vlan-mode-type
          native-vlan: null  # oc-vlan-types:vlan-id
          access-vlan: null  # oc-vlan-types:vlan-id
          trunk-vlans: []  # leaf-list, union
    "openconfig-vlan:routed-vlan":
      config:
        vlan: null  # union
"openconfig-keychain:keychains":
  keychain:
  - name: null  # leafref
    config:
      name: null  # string
      tolerance: null  # union
    keys:
      key:
      - key-id: null  # leafref
        config:
          key-id: null  # union
          secret-key: null  # string
          crypto-algorithm: null  # identityref base oc-keychain-types:CRYPTO_TYPE
        send-lifetime:
          config:
            start-time: null  # oc-types:timeticks64
            end-time: null  # oc-types:timeticks64
            send-and-receive: true  # boolean
        receive-lifetime:
          config:
            start-time: null  # oc-types:timeticks64
            end-time: null  # oc-types:timeticks64
"openconfig-network-instance:network-instances":
  network-instance:
  - name: null  # leafref
    config:
      name: null  # string
      type: null  # identityref base oc-ni-types:NETWORK_INSTANCE_TYPE, mandatory
      description: null  # string
      router-id: null  # oc-yang:dotted-quad
      route-distinguisher: null  # oc-ni-types:route-distinguisher
      fallback-network-instance: null  # network-instance-ref
    fdb:
      config:
        mac-learning: null  # boolean
        mac-aging-time: null  # uint16
        maximum-entries: null  # uint16
        anycast-gateway-mac: null  # oc-yang:mac-address
        flood-unknown-unicast-supression: false  # boolean
      mac-table:
        entries:
          entry:
          - mac-address: null  # leafref
            vlan: null  # leafref
            config:
              mac-address: null  # oc-yang:mac-address
              vlan: null  # leafref
            interface:
              interface-ref:
                config:
                  interface: null  # leafref
                  subinterface: null  # leafref
      mac-mobility:
        config:
          mac-mobility: null  # boolean
          mac-mobility-window: 180  # uint16
          mac-mobility-threshold: 5  # uint8
          ip-mobility-threshold: null  # uint16
          duplicate-ip-detection-interval: null  # uint16
      arp-proxy:
        config:
          enable: false  # boolean
          arp-suppression: false  # boolean
          ip-mobility-threshold: null  # uint16
          duplicate-ip-detection-interval: null  # uint16
      nd-proxy:
        config:
          enable: false  # boolean
          nd-suppression: false  # boolean
          ip-mobility-threshold: null  # uint16
          duplicate-ip-detection-interval: null  # uint16
    evpn:
      evpn-instances:
        evpn-instance:
        - evi: null  # leafref
          config:
            evi: null  # string
            encapsulation-type: null  # identityref base oc-ni-types:ENCAPSULATION
            service-type: null  # identityref base oc-evpn-types:EVPN_TYPE
            multicast-group: null  # oc-inet:ip-address
            multicast-mask: null  # oc-inet:ip-address
            replication-mode: null  # enumeration, enum: STATIC_INGRESS_REPLICATION|BGP|MULTICAST
            route-distinguisher: null  # union
            control-word-enabled: null  # boolean
            local-vpws-service-id: null  # uint32
            remote-vpws-service-id: null  # uint32
          import-export-policy:
            config:
              export-route-target: []  # leaf-list, union
              import-route-target: []  # leaf-list, union
          vxlan:
            config:
              vni: null  # oc-evpn-types:vni-id
              vni-list: []  # leaf-list, oc-evpn-types:vni-id
              overlay-endpoint-network-instance: null  # leafref
              overlay-endpoint: null  # leafref
              host-reachability-bgp: null  # boolean
              multicast-group: null  # oc-inet:ip-address
              multicast-mask: null  # oc-inet:ip-address
            anycast-source-interface:
              config:
                interface: null  # leafref
                subinterface: null  # leafref
          pbb:
            b-component:
            - b-component-name: null  # leafref
              config:
                b-component-name: null  # string
                backbone-src-mac: null  # oc-yang:mac-address
              i-components:
                i-component:
                - i-sid: null  # leafref
                  config:
                    i-sid: null  # uint32
    encapsulation:
      config:
        encapsulation-type: null  # identityref base oc-ni-types:ENCAPSULATION
        label-allocation-mode: null  # identityref base oc-ni-types:LABEL_ALLOCATION_MODE
        control-word: null  # boolean
    inter-instance-policies:
      apply-policy:
        config:
          import-policy: []  # leaf-list, leafref
          export-policy: []  # leaf-list, leafref
          default-import-policy: REJECT_ROUTE  # default-policy-type
          default-export-policy: REJECT_ROUTE  # default-policy-type
      import-export-policy:
        config:
          export-route-target: []  # leaf-list, union
          import-route-target: []  # leaf-list, union
    table-connections:
      table-connection:
      - src-protocol: null  # leafref
        dst-protocol: null  # leafref
        address-family: null  # leafref
        config:
          src-protocol: null  # leafref
          address-family: null  # leafref
          dst-protocol: null  # leafref
          disable-metric-propagation: false  # boolean
          import-policy: []  # leaf-list, leafref
          default-import-policy: REJECT_ROUTE  # default-policy-type
    interfaces:
      interface:
      - id: null  # leafref
        config:
          id: null  # oc-if:interface-id
          interface: null  # leafref
          subinterface: null  # leafref
          associated-address-families: []  # leaf-list, identityref base oc-types:ADDRESS_FAMILY
          mac-pinning: null  # boolean
          irb-anycast-gateway: null  # enumeration, enum: DISTRIBUTED_SYMETRIC|DISTRIBUTED_ASYMETRIC|HYBRID|CENTRALIZED
    route-limits:
      route-limit:
      - afi: null  # leafref
        config:
          afi: null  # identityref base octypes:ADDRESS_FAMILY
          maximum: null  # uint32
          warning-only: false  # boolean
          alarm-threshold: null  # uint32
    tables:
      table:
      - protocol: null  # leafref
        address-family: null  # leafref
        config:
          protocol: null  # leafref
          address-family: null  # identityref base oc-types:ADDRESS_FAMILY
    connection-points:
      connection-point:
      - connection-point-id: null  # leafref
        config:
          connection-point-id: null  # string
        endpoints:
          endpoint:
          - endpoint-id: null  # leafref
            config:
              endpoint-id: null  # string
              precedence: null  # uint16
              type: null  # identityref base oc-ni-types:ENDPOINT_TYPE
            local:
              config:
                interface: null  # leafref
                subinterface: null  # leafref
                site-id: null  # uint16
                site-label-block-offset: null  # uint16
                site-label-block-size: null  # uint16
            remote:
              config:
                remote-system: null  # oc-inet:ip-address
                virtual-circuit-identifier: null  # uint32
                site-id: null  # uint16
            vxlan:
              config:
                description: null  # string
                enabled: null  # boolean
                source-interface: null  # string
              local-endpoint-vnis:
                local-endpoint-vni:
                - vni: null  # leafref
                  config:
                    vni: null  # oc-evpn-types:evi-id
                    vni-type: null  # enumeration, enum: L2|L3
                    bridge-domain: null  # uint32
                    l3-vrf-name: null  # string
    mpls:
      global:
        config:
          null-label: oc-mplst:IMPLICIT  # identityref base oc-mplst:NULL_LABEL_TYPE
          ttl-propagation: true  # boolean
          pw-encapsulation: null  # identityref base oc-mplst:PSEUDOWIRE_ENCAPSULATION
        interface-attributes:
          interface:
          - interface-id: null  # leafref
            config:
              interface-id: null  # oc-if:interface-id
              mpls-enabled: false  # boolean
            interface-ref:
              config:
                interface: null  # leafref
                subinterface: null  # leafref
        reserved-label-blocks:
          reserved-label-block:
          - local-id: null  # leafref
            config:
              local-id: null  # string
              lower-bound: null  # oc-mplst:mpls-label
              upper-bound: null  # oc-mplst:mpls-label
      te-global-attributes:
        srlgs:
          srlg:
          - name: null  # leafref
            config:
              name: null  # string
              value: null  # uint32
              cost: null  # uint32
              flooding-type: FLOODED_SRLG  # mpls-srlg-flooding-type
            static-srlg-members:
              members-list:
              - from-address: null  # leafref
                config:
                  from-address: null  # inet:ip-address
                  to-address: null  # inet:ip-address
        mpls-admin-groups:
          admin-group:
          - admin-group-name: null  # leafref
            config:
              admin-group-name: null  # string
              bit-position: null  # uint32
        te-lsp-timers:
          config:
            install-delay: null  # uint16
            cleanup-delay: null  # uint16
            reoptimize-timer: null  # uint16
      te-interface-attributes:
        interface:
        - interface-id: null  # leafref
          config:
            interface-id: null  # oc-if:interface-id
            te-metric: null  # uint32
            srlg-membership: []  # leaf-list, leafref
            admin-group: []  # leaf-list, string
          interface-ref:
            config:
              interface: null  # leafref
              subinterface: null  # leafref
          igp-flooding-bandwidth:
            config:
              threshold-type: null  # enumeration, enum: DELTA|THRESHOLD_CROSSED
              delta-percentage: null  # oc-types:percentage
              threshold-specification: null  # enumeration, enum: MIRRORED_UP_DOWN|SEPARATE_UP_DOWN
              up-thresholds: []  # leaf-list, oc-types:percentage
              down-thresholds: []  # leaf-list, oc-types:percentage
              up-down-thresholds: []  # leaf-list, oc-types:percentage
      signaling-protocols:
        rsvp-te:
          sessions:
          neighbors:
          global:
            graceful-restart:
              config:
                enable: false  # boolean
                restart-time: null  # uint32
                recovery-time: null  # uint32
            soft-preemption:
              config:
                enable: false  # boolean
                soft-preemption-timeout: 30  # uint16
            hellos:
              config:
                hello-interval: 9000  # uint16
                refresh-reduction: true  # boolean
          interface-attributes:
            interface:
            - interface-id: null  # leafref
              config:
                interface-id: null  # oc-if:interface-id
              interface-ref:
                config:
                  interface: null  # leafref
                  subinterface: null  # leafref
              bandwidth-reservations:
              hellos:
                config:
                  hello-interval: 9000  # uint16
                  refresh-reduction: true  # boolean
              authentication:
                config:
                  enable: false  # boolean
                  authentication-type: null  # identityref base oc-mplst:RSVP_AUTH_TYPE
                  authentication-key: null  # oc-types:routing-password
              subscription:
                config:
                  subscription: null  # oc-types:percentage
              protection:
                config:
                  link-protection-style-requested: oc-mplst:LINK_NODE_PROTECTION_REQUESTED  # identityref base oc-mplst:PROTECTION_TYPE
                  bypass-optimize-interval: null  # uint16
                  bypass-tunnel: []  # leaf-list, leafref
        ldp:
          global:
            config:
              lsr-id: null  # oc-inet:ip-address
            graceful-restart:
              config:
                enabled: false  # boolean
                reconnect-time: null  # uint16
                recovery-time: null  # uint16
                forwarding-holdtime: null  # uint16
                helper-enable: null  # boolean
            authentication:
              config:
                enable: false  # boolean
                authentication-key: null  # oc-types:routing-password
          interface-attributes:
            config:
              hello-holdtime: null  # uint16
              hello-interval: null  # uint16
            interfaces:
              interface:
              - interface-id: null  # leafref
                config:
                  interface-id: null  # oc-if:interface-id
                  hello-holdtime: null  # uint16
                  hello-interval: null  # uint16
                interface-ref:
                  config:
                    interface: null  # leafref
                    subinterface: null  # leafref
                address-families:
                  address-family:
                  - afi-name: null  # leafref
                    config:
                      afi-name: null  # oc-ldp:mpls-ldp-afi
                      enabled: false  # boolean
          targeted:
            config:
              hello-holdtime: null  # uint16
              hello-interval: null  # uint16
              hello-accept: null  # boolean
            address-families:
              address-family:
              - afi-name: null  # leafref
                config:
                  afi-name: null  # oc-ldp:mpls-ldp-afi
                targets:
                  target:
                  - remote-address: null  # leafref
                    config:
                      remote-address: null  # oc-inet:ip-address
                      local-address: null  # oc-inet:ip-address
                      enabled: false  # boolean
                      hello-holdtime: null  # uint16
                      hello-interval: null  # uint16
          neighbors:
            neighbor:
            - lsr-id: null  # leafref
              label-space-id: null  # leafref
              config:
                lsr-id: null  # oc-inet:ip-address
                label-space-id: null  # uint16
                enable-downstream-on-demand: false  # boolean
              authentication:
                config:
                  enable: false  # boolean
                  authentication-key: null  # oc-types:routing-password
        segment-routing:
          aggregate-sid-counters:
          interfaces:
            interface:
            - interface-id: null  # leafref
              config:
                interface-id: null  # oc-if:interface-id
              sid-counters:
              interface-ref:
                config:
                  interface: null  # leafref
                  subinterface: null  # leafref
      lsps:
        constrained-path:
          named-explicit-paths:
            named-explicit-path:
            - name: null  # leafref
              config:
                name: null  # string
                sid-selection-mode: MIXED_MODE  # enumeration, enum: ADJ_SID_ONLY|MIXED_MODE
                sid-protection-required: false  # boolean
              explicit-route-objects:
                explicit-route-object:
                - index: null  # leafref
                  config:
                    address: null  # inet:ip-address
                    hop-type: null  # mpls-hop-type
                    index: null  # uint8
          tunnels:
            tunnel:
            - name: null  # leafref
              config:
                name: null  # string
                type: null  # identityref base oc-mplst:TUNNEL_TYPE
                signaling-protocol: null  # identityref base oc-mplst:PATH_SETUP_PROTOCOL
                description: null  # string
                admin-status: oc-mplst:ADMIN_UP  # identityref base oc-mplst:TUNNEL_ADMIN_STATUS
                preference: null  # uint8
                metric-type: oc-mplst:LSP_METRIC_INHERITED  # identityref base oc-mplst:LSP_METRIC_TYPE
                metric: null  # int32
                shortcut-eligible: true  # boolean
                protection-style-requested: oc-mplst:UNPROTECTED  # identityref base oc-mplst:PROTECTION_TYPE
                reoptimize-timer: null  # uint16
                source: null  # inet:ip-address
                soft-preemption: false  # boolean
                setup-priority: 7  # uint8
                hold-priority: 0  # uint8
              bandwidth:
                config:
                  specification-type: SPECIFIED  # te-bandwidth-type
                  set-bandwidth: null  # oc-mplst:bandwidth-kbps
                auto-bandwidth:
                  config:
                    enabled: false  # boolean
                    min-bw: null  # oc-mplst:bandwidth-kbps
                    max-bw: null  # oc-mplst:bandwidth-kbps
                    adjust-interval: null  # uint32
                    adjust-threshold: null  # oc-types:percentage
                  overflow:
                    config:
                      enabled: false  # boolean
                      overflow-threshold: null  # oc-types:percentage
                      trigger-event-count: null  # uint16
                  underflow:
                    config:
                      enabled: false  # boolean
                      underflow-threshold: null  # oc-types:percentage
                      trigger-event-count: null  # uint16
              p2p-tunnel-attributes:
                config:
                  destination: null  # inet:ip-address
                  record-route-enabled: null  # boolean
                p2p-primary-path:
                  p2p-primary-path:
                  - name: null  # leafref
                    config:
                      name: null  # string
                      path-computation-method: oc-mplst:LOCALLY_COMPUTED  # identityref base oc-mplst:PATH_COMPUTATION_METHOD
                      use-cspf: null  # boolean
                      cspf-tiebreaker: null  # cspf-tie-breaking
                      path-computation-server: null  # leafref
                      path-control: null  # oc-pcep:lsp-control-type
                      explicit-path-name: null  # leafref
                      preference: null  # uint8
                      setup-priority: 7  # uint8
                      hold-priority: 0  # uint8
                      retry-timer: null  # uint16
                    path-metric-bound-constraints:
                      path-metric-bound-constraint:
                      - type: null  # leafref
                        config:
                          type: null  # identityref base oc-mplst:PATH_METRIC_TYPE
                          metric-upper-bound: 0  # uint64
                    candidate-secondary-paths:
                      candidate-secondary-path:
                      - secondary-path: null  # leafref
                        config:
                          secondary-path: null  # leafref
                          priority: null  # uint16
                    admin-groups:
                      config:
                        exclude-group: []  # leaf-list, leafref
                        include-all-group: []  # leaf-list, leafref
                        include-any-group: []  # leaf-list, leafref
                p2p-secondary-paths:
                  p2p-secondary-path:
                  - name: null  # leafref
                    config:
                      name: null  # string
                      path-computation-method: oc-mplst:LOCALLY_COMPUTED  # identityref base oc-mplst:PATH_COMPUTATION_METHOD
                      use-cspf: null  # boolean
                      cspf-tiebreaker: null  # cspf-tie-breaking
                      path-computation-server: null  # leafref
                      path-control: null  # oc-pcep:lsp-control-type
                      explicit-path-name: null  # leafref
                      preference: null  # uint8
                      setup-priority: 7  # uint8
                      hold-priority: 0  # uint8
                      retry-timer: null  # uint16
                    path-metric-bound-constraints:
                      path-metric-bound-constraint:
                      - type: null  # leafref
                        config:
                          type: null  # identityref base oc-mplst:PATH_METRIC_TYPE
                          metric-upper-bound: 0  # uint64
                    admin-groups:
                      config:
                        exclude-group: []  # leaf-list, leafref
                        include-all-group: []  # leaf-list, leafref
                        include-any-group: []  # leaf-list, leafref
        unconstrained-path:
          path-setup-protocol:
            ldp:
        static-lsps:
          static-lsp:
          - name: null  # leafref
            config:
              name: null  # string
            ingress:
              config:
                incoming-label: null  # oc-mplst:mpls-label
                next-hop: null  # inet:ip-address
                push-label: null  # oc-mplst:mpls-label
                interface: null  # leafref
                subinterface: null  # leafref
                metric: null  # uint8
              lsp-next-hops:
                lsp-next-hop:
                - index: null  # leafref
                  config:
                    index: null  # uint32
                    ip-address: null  # inet:ip-address
                    push-label: null  # oc-mplst:mpls-label
                    interface: null  # leafref
                    subinterface: null  # leafref
            transit:
              config:
                incoming-label: null  # oc-mplst:mpls-label
                next-hop: null  # inet:ip-address
                push-label: null  # oc-mplst:mpls-label
                interface: null  # leafref
                subinterface: null  # leafref
                metric: null  # uint8
              lsp-next-hops:
                lsp-next-hop:
                - index: null  # leafref
                  config:
                    index: null  # uint32
                    ip-address: null  # inet:ip-address
                    push-label: null  # oc-mplst:mpls-label
                    interface: null  # leafref
                    subinterface: null  # leafref
            egress:
              config:
                incoming-label: null  # oc-mplst:mpls-label
                next-hop: null  # inet:ip-address
                push-label: null  # oc-mplst:mpls-label
                interface: null  # leafref
                subinterface: null  # leafref
                metric: null  # uint8
              lsp-next-hops:
                lsp-next-hop:
                - index: null  # leafref
                  config:
                    index: null  # uint32
                    ip-address: null  # inet:ip-address
                    push-label: null  # oc-mplst:mpls-label
                    interface: null  # leafref
                    subinterface: null  # leafref
    segment-routing:
      srgbs:
        srgb:
        - local-id: null  # leafref
          config:
            local-id: null  # string
            dataplane-type: null  # oc-srt:sr-dataplane-type
            mpls-label-blocks: []  # leaf-list, leafref
            ipv6-prefixes: []  # leaf-list, inet:ipv6-prefix
      srlbs:
        srlb:
        - local-id: null  # leafref
          config:
            local-id: null  # string
            dataplane-type: null  # oc-srt:sr-dataplane-type
            mpls-label-block: null  # leafref
            ipv6-prefix: null  # inet:ipv6-prefix
    vlans:
      vlan:
      - vlan-id: null  # leafref
        config:
          vlan-id: null  # oc-vlan-types:vlan-id
          name: null  # string
          status: ACTIVE  # enumeration, enum: ACTIVE|SUSPENDED
        members:
    policy-forwarding:
      config:
        global-decap-policy: null  # string
      policies:
        policy:
        - policy-id: null  # leafref
          policy-counters:
            policy-counter:
            - name: null  # leafref
              config:
                name: null  # string
          config:
            policy-id: null  # string
            type: PBR_POLICY  # enumeration, enum: PBR_POLICY|VRF_SELECTION_POLICY|MATCH_ACTION_POLICY
          rules:
            rule:
            - sequence-id: null  # leafref
              config:
                sequence-id: null  # uint32
                address-family: null  # identityref base oc-types:ADDRESS_FAMILY
                description: null  # string
              l2:
                config:
                  source-mac: null  # oc-yang:mac-address
                  source-mac-mask: null  # oc-yang:mac-address
                  destination-mac: null  # oc-yang:mac-address
                  destination-mac-mask: null  # oc-yang:mac-address
                  ethertype: null  # oc-pkt-match-types:ethertype-type
              ipv4:
                config:
                  source-address: null  # oc-inet:ipv4-prefix
                  source-address-prefix-set: null  # leafref
                  destination-address: null  # oc-inet:ipv4-prefix
                  destination-address-prefix-set: null  # leafref
                  fragment-offsets: []  # leaf-list, oc-pkt-match-types:fragment-offset-range
                  dscp: null  # oc-inet:dscp
                  dscp-set: []  # leaf-list, oc-inet:dscp
                  length: null  # uint16
                  protocol: null  # oc-pkt-match-types:ip-protocol-type
                  hop-limit: null  # uint8
                icmpv4:
                  config:
                    type: null  # identityref base oc-icmpv4-types:TYPE
                    code: null  # identityref base oc-icmpv4-types:CODE
              ipv6:
                config:
                  source-address: null  # oc-inet:ipv6-prefix
                  source-address-prefix-set: null  # leafref
                  source-flow-label: null  # oc-inet:ipv6-flow-label
                  destination-address: null  # oc-inet:ipv6-prefix
                  destination-address-prefix-set: null  # leafref
                  destination-flow-label: null  # oc-inet:ipv6-flow-label
                  flow-label: null  # oc-inet:ipv6-flow-label
                  dscp: null  # oc-inet:dscp
                  dscp-set: []  # leaf-list, oc-inet:dscp
                  length: null  # uint16
                  protocol: null  # oc-pkt-match-types:ip-protocol-type
                  hop-limit: null  # uint8
                icmpv6:
                  config:
                    type: null  # identityref base oc-icmpv6-types:TYPE
                    code: null  # identityref base oc-icmpv6-types:CODE
              transport:
                config:
                  source-port: null  # oc-pkt-match-types:port-num-range
                  source-port-set: null  # leafref
                  destination-port: null  # oc-pkt-match-types:port-num-range
                  destination-port-set: null  # leafref
                  detail-mode: null  # enumeration, enum: EXPLICIT|BUILTIN
                  explicit-detail-match-mode: null  # enumeration, enum: ANY|ALL
                  explicit-tcp-flags: []  # leaf-list, identityref base oc-pkt-match-types:TCP_FLAGS
                  builtin-detail: null  # enumeration, enum: TCP_INITIAL|TCP_ESTABLISHED|FRAGMENT
              action:
                config:
                  count: null  # leafref
                  discard: false  # boolean
                  decapsulate-gre: false  # boolean
                  decap-network-instance: null  # leafref
                  decap-fallback-network-instance: null  # leafref
                  log: null  # boolean
                  post-decap-network-instance: null  # leafref
                  network-instance: null  # leafref
                  path-selection-group: null  # leafref
                  next-hop: null  # oc-inet:ip-address
                  next-hop-group: null  # leafref
                  decapsulate-mpls-in-udp: false  # boolean
                  decapsulate-gue: false  # boolean
                  ip-ttl: null  # uint8
                encapsulate-gre:
                  config:
                    identifying-prefix: null  # oc-inet:ip-prefix
                  targets:
                    target:
                    - id: null  # leafref
                      config:
                        id: null  # string
                        source: null  # oc-inet:ip-address
                        destination: null  # oc-inet:ip-prefix
                        ip-ttl: null  # uint8
                ipv4:
                  config:
                    dscp: null  # oc-inet-types:dscp
                    ttl: null  # uint8
                ipv6:
                  config:
                    dscp: null  # oc-inet-types:dscp
                    hop-limit: null  # uint8
                mpls:
                  config:
                    mpls-traffic-class: null  # oc-mplst:mpls-tc
                forwarding:
                  config:
                    forwarding-action: FORWARD  # identityref base FORWARDING_ACTION
                    qos-forwarding-group: null  # leafref
      interfaces:
        interface:
        - interface-id: null  # leafref
          config:
            interface-id: null  # oc-if:interface-id
            apply-forwarding-policy: null  # leafref
            apply-vrf-selection-policy: null  # leafref
          interface-ref:
            config:
              interface: null  # leafref
              subinterface: null  # leafref
      path-selection-groups:
        path-selection-group:
        - group-id: null  # leafref
          config:
            group-id: null  # string
            mpls-lsp: []  # leaf-list, leafref
    static:
      next-hop-groups:
        next-hop-group:
        - name: null  # leafref
          config:
            name: null  # string
          next-hops:
            next-hop:
            - index: null  # leafref
              config:
                index: null  # leafref
      next-hops:
        next-hop:
        - index: null  # leafref
          config:
            index: null  # string
            next-hop: null  # union
            recurse: false  # boolean
            metric: null  # uint32
            preference: null  # uint32
            wecmp-weight: null  # union
          encap-headers:
            encap-header:
            - index: null  # leafref
              config:
                index: null  # uint8
                type: null  # oc-aftt:encapsulation-header-type
              udp-v4:
                config:
                  src-ip: null  # oc-inet:ipv4-address
                  dst-ip: null  # oc-inet:ipv4-address
                  dscp: null  # oc-inet:dscp
                  src-udp-port: null  # oc-inet:port-number
                  dst-udp-port: null  # oc-inet:port-number
                  ip-ttl: null  # uint8
              udp-v6:
                config:
                  src-ip: null  # oc-inet:ipv6-address
                  dst-ip: null  # oc-inet:ipv6-address
                  dscp: null  # oc-inet:dscp
                  src-udp-port: null  # oc-inet:port-number
                  dst-udp-port: null  # oc-inet:port-number
                  ip-ttl: null  # uint8
              gre:
                config:
                  src-ip: null  # oc-inet:ip-address
                  dst-ip: null  # oc-inet:ip-address
                  ttl: null  # uint8
              mpls:
                config:
                  traffic-class: null  # oc-mplst:mpls-tc
                  label: null  # oc-mplst:mpls-label
    afts:
    protocols:
      protocol:
      - identifier: null  # leafref
        name: null  # leafref
        config:
          identifier: null  # identityref base oc-pol-types:INSTALL_PROTOCOL_TYPE
          name: DEFAULT  # string
          enabled: null  # boolean
          default-metric: null  # uint32
        static-routes:
          static:
          - prefix: null  # leafref
            config:
              prefix: null  # inet:ip-prefix
              set-tag: null  # oc-pt:tag-type
              description: null  # string
            next-hops:
              next-hop:
              - index: null  # leafref
                config:
                  index: null  # string
                  next-hop: null  # union
                  recurse: false  # boolean
                  metric: null  # uint32
                  preference: null  # uint32
                  wecmp-weight: null  # union
                enable-bfd:
                  config:
                    enabled: null  # boolean
                    desired-minimum-tx-interval: null  # uint32
                    required-minimum-receive: null  # uint32
                    detection-multiplier: null  # uint8
                interface-ref:
                  config:
                    interface: null  # leafref
                    subinterface: null  # leafref
        local-aggregates:
          aggregate:
          - prefix: null  # leafref
            config:
              prefix: null  # inet:ip-prefix
              discard: false  # boolean
              set-tag: null  # oc-pt:tag-type
              description: null  # string
              metric: null  # uint32
              preference: null  # uint32
              wecmp-weight: null  # union
        bgp:
          global:
            config:
              as: null  # oc-inet:as-number, mandatory
              router-id: null  # oc-yang:dotted-quad
            default-route-distance:
              config:
                external-route-distance: null  # uint8
                internal-route-distance: null  # uint8
            confederation:
              config:
                identifier: null  # oc-inet:as-number
                member-as: []  # leaf-list, oc-inet:as-number
            graceful-restart:
              config:
                enabled: null  # boolean
                restart-time: null  # uint16
                stale-routes-time: null  # uint16
                helper-only: null  # boolean
            use-multiple-paths:
              config:
                enabled: false  # boolean
              ebgp:
                link-bandwidth-ext-community:
                  config:
                    enabled: null  # boolean
                config:
                  allow-multiple-as: false  # boolean
                  maximum-paths: 1  # uint32
              ibgp:
                link-bandwidth-ext-community:
                  config:
                    enabled: null  # boolean
                config:
                  maximum-paths: 1  # uint32
            route-selection-options:
              config:
                always-compare-med: false  # boolean
                ignore-as-path-length: false  # boolean
                external-compare-router-id: true  # boolean
                advertise-inactive-routes: false  # boolean
                enable-aigp: false  # boolean
                ignore-next-hop-igp-metric: false  # boolean
            afi-safis:
              afi-safi:
              - afi-safi-name: null  # leafref
                config:
                  afi-safi-name: null  # identityref base oc-bgp-types:AFI_SAFI_TYPE
                  enabled: false  # boolean
                  send-community-type: []  # leaf-list, oc-bgp-types:community-type
                graceful-restart:
                  config:
                    enabled: false  # boolean
                route-selection-options:
                  config:
                    always-compare-med: false  # boolean
                    ignore-as-path-length: false  # boolean
                    external-compare-router-id: true  # boolean
                    advertise-inactive-routes: false  # boolean
                    enable-aigp: false  # boolean
                    ignore-next-hop-igp-metric: false  # boolean
                use-multiple-paths:
                  config:
                    enabled: false  # boolean
                  ebgp:
                    link-bandwidth-ext-community:
                      config:
                        enabled: null  # boolean
                    config:
                      allow-multiple-as: false  # boolean
                      maximum-paths: 1  # uint32
                  ibgp:
                    link-bandwidth-ext-community:
                      config:
                        enabled: null  # boolean
                    config:
                      maximum-paths: 1  # uint32
                add-paths:
                  config:
                    receive: false  # boolean
                    send: false  # boolean
                    send-max: null  # uint8
                    eligible-prefix-policy: null  # leafref
                ipv4-unicast:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  config:
                    send-default-route: false  # boolean
                    extended-next-hop-encoding: false  # boolean
                ipv6-unicast:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  config:
                    send-default-route: false  # boolean
                ipv4-labeled-unicast:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                ipv6-labeled-unicast:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                l3vpn-ipv4-unicast:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                l3vpn-ipv6-unicast:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                l3vpn-ipv4-multicast:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                l3vpn-ipv6-multicast:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                l2vpn-vpls:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                l2vpn-evpn:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                srte-policy-ipv4:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                srte-policy-ipv6:
                  prefix-limit:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                  prefix-limit-received:
                    config:
                      max-prefixes: null  # uint32
                      prevent-teardown: false  # boolean
                      warning-threshold-pct: null  # oc-types:percentage
                apply-policy:
                  config:
                    default-import-policy: REJECT_ROUTE  # default-policy-type
                    default-export-policy: REJECT_ROUTE  # default-policy-type
            dynamic-neighbor-prefixes:
              dynamic-neighbor-prefix:
              - prefix: null  # leafref
                config:
                  prefix: null  # oc-inet:ip-prefix
                  peer-group: null  # leafref
            apply-policy:
              config:
                default-import-policy: REJECT_ROUTE  # default-policy-type
                default-export-policy: REJECT_ROUTE  # default-policy-type
          neighbors:
            neighbor:
            - neighbor-address: null  # leafref
              config:
                peer-group: null  # leafref
                neighbor-address: null  # oc-inet:ip-address
                neighbor-port: 179  # oc-inet:port-number
                enabled: true  # boolean
                peer-as: null  # oc-inet:as-number
                local-as: null  # oc-inet:as-number
                peer-type: null  # oc-bgp-types:peer-type
                auth-password: null  # oc-types:routing-password
                remove-private-as: null  # oc-bgp-types:remove-private-as-option
                route-flap-damping: false  # boolean
                send-community: NONE  # oc-bgp-types:community-type
                send-community-type: []  # leaf-list, oc-bgp-types:community-type
                description: null  # string
              timers:
                config:
                  connect-retry: 30  # uint16
                  hold-time: 90  # uint16
                  keepalive-interval: 30  # uint16
                  minimum-advertisement-interval: 30  # uint16
                  restart-time: null  # uint16
              transport:
                config:
                  tcp-mss: null  # uint16
                  mtu-discovery: false  # boolean
                  passive-mode: false  # boolean
                  local-address: null  # union
              error-handling:
                config:
                  treat-as-withdraw: false  # boolean
              graceful-restart:
                config:
                  enabled: null  # boolean
                  restart-time: null  # uint16
                  stale-routes-time: null  # uint16
                  helper-only: null  # boolean
              logging-options:
                config:
                  log-neighbor-state-changes: true  # boolean
              ebgp-multihop:
                config:
                  enabled: false  # boolean
                  multihop-ttl: null  # uint8
              route-reflector:
                config:
                  route-reflector-cluster-id: null  # oc-bgp-types:rr-cluster-id-type
                  route-reflector-client: false  # boolean
              as-path-options:
                config:
                  allow-own-as: 0  # uint8
                  replace-peer-as: false  # boolean
                  disable-peer-as-filter: false  # boolean
              auto-link-bandwidth:
                import:
                  config:
                    enabled: false  # boolean
                    hold-down-time: null  # uint16
                    transitive: false  # boolean
              use-multiple-paths:
                config:
                  enabled: false  # boolean
                ebgp:
                  config:
                    allow-multiple-as: false  # boolean
              apply-policy:
                config:
                  import-policy: []  # leaf-list, leafref
                  export-policy: []  # leaf-list, leafref
                  default-import-policy: REJECT_ROUTE  # default-policy-type
                  default-export-policy: REJECT_ROUTE  # default-policy-type
              afi-safis:
                afi-safi:
                - afi-safi-name: null  # leafref
                  config:
                    afi-safi-name: null  # identityref base oc-bgp-types:AFI_SAFI_TYPE
                    enabled: null  # boolean
                    send-community-type: []  # leaf-list, oc-bgp-types:community-type
                  graceful-restart:
                    config:
                      enabled: false  # boolean
                  add-paths:
                    config:
                      receive: false  # boolean
                      send: false  # boolean
                      send-max: null  # uint8
                      eligible-prefix-policy: null  # leafref
                  apply-policy:
                    config:
                      import-policy: []  # leaf-list, leafref
                      export-policy: []  # leaf-list, leafref
                      default-import-policy: REJECT_ROUTE  # default-policy-type
                      default-export-policy: REJECT_ROUTE  # default-policy-type
                  ipv4-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    config:
                      send-default-route: false  # boolean
                      extended-next-hop-encoding: false  # boolean
                      enable-aigp: null  # boolean
                  ipv6-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    config:
                      send-default-route: false  # boolean
                      enable-aigp: null  # boolean
                  ipv4-labeled-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  ipv6-labeled-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l3vpn-ipv4-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l3vpn-ipv6-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l3vpn-ipv4-multicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l3vpn-ipv6-multicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l2vpn-vpls:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l2vpn-evpn:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  srte-policy-ipv4:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  srte-policy-ipv6:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  use-multiple-paths:
                    config:
                      enabled: false  # boolean
                    ebgp:
                      config:
                        allow-multiple-as: false  # boolean
              enable-bfd:
                config:
                  enabled: null  # boolean
                  desired-minimum-tx-interval: null  # uint32
                  required-minimum-receive: null  # uint32
                  detection-multiplier: null  # uint8
          peer-groups:
            peer-group:
            - peer-group-name: null  # leafref
              config:
                peer-group-name: null  # string
                peer-as: null  # oc-inet:as-number
                local-as: null  # oc-inet:as-number
                peer-type: null  # oc-bgp-types:peer-type
                auth-password: null  # oc-types:routing-password
                remove-private-as: null  # oc-bgp-types:remove-private-as-option
                route-flap-damping: false  # boolean
                send-community: NONE  # oc-bgp-types:community-type
                send-community-type: []  # leaf-list, oc-bgp-types:community-type
                description: null  # string
              timers:
                config:
                  connect-retry: 30  # uint16
                  hold-time: 90  # uint16
                  keepalive-interval: 30  # uint16
                  minimum-advertisement-interval: 30  # uint16
                  restart-time: null  # uint16
              transport:
                config:
                  tcp-mss: null  # uint16
                  mtu-discovery: false  # boolean
                  passive-mode: false  # boolean
                  local-address: null  # union
              error-handling:
                config:
                  treat-as-withdraw: false  # boolean
              graceful-restart:
                config:
                  enabled: null  # boolean
                  restart-time: null  # uint16
                  stale-routes-time: null  # uint16
                  helper-only: null  # boolean
              logging-options:
                config:
                  log-neighbor-state-changes: true  # boolean
              ebgp-multihop:
                config:
                  enabled: false  # boolean
                  multihop-ttl: null  # uint8
              route-reflector:
                config:
                  route-reflector-cluster-id: null  # oc-bgp-types:rr-cluster-id-type
                  route-reflector-client: false  # boolean
              as-path-options:
                config:
                  allow-own-as: 0  # uint8
                  replace-peer-as: false  # boolean
                  disable-peer-as-filter: false  # boolean
              auto-link-bandwidth:
                import:
                  config:
                    enabled: false  # boolean
                    hold-down-time: null  # uint16
                    transitive: false  # boolean
              use-multiple-paths:
                config:
                  enabled: false  # boolean
                ebgp:
                  link-bandwidth-ext-community:
                    config:
                      enabled: null  # boolean
                  config:
                    allow-multiple-as: false  # boolean
                    maximum-paths: 1  # uint32
                ibgp:
                  link-bandwidth-ext-community:
                    config:
                      enabled: null  # boolean
                  config:
                    maximum-paths: 1  # uint32
              apply-policy:
                config:
                  import-policy: []  # leaf-list, leafref
                  export-policy: []  # leaf-list, leafref
                  default-import-policy: REJECT_ROUTE  # default-policy-type
                  default-export-policy: REJECT_ROUTE  # default-policy-type
              afi-safis:
                afi-safi:
                - afi-safi-name: null  # leafref
                  config:
                    afi-safi-name: null  # identityref base oc-bgp-types:AFI_SAFI_TYPE
                    enabled: null  # boolean
                    send-community-type: []  # leaf-list, oc-bgp-types:community-type
                  graceful-restart:
                    config:
                      enabled: false  # boolean
                  add-paths:
                    config:
                      receive: false  # boolean
                      send: false  # boolean
                      send-max: null  # uint8
                      eligible-prefix-policy: null  # leafref
                  use-multiple-paths:
                    config:
                      enabled: false  # boolean
                    ebgp:
                      link-bandwidth-ext-community:
                        config:
                          enabled: null  # boolean
                      config:
                        allow-multiple-as: false  # boolean
                        maximum-paths: 1  # uint32
                    ibgp:
                      link-bandwidth-ext-community:
                        config:
                          enabled: null  # boolean
                      config:
                        maximum-paths: 1  # uint32
                  apply-policy:
                    config:
                      import-policy: []  # leaf-list, leafref
                      export-policy: []  # leaf-list, leafref
                      default-import-policy: REJECT_ROUTE  # default-policy-type
                      default-export-policy: REJECT_ROUTE  # default-policy-type
                  ipv4-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    config:
                      send-default-route: false  # boolean
                      extended-next-hop-encoding: false  # boolean
                      enable-aigp: null  # boolean
                  ipv6-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    config:
                      send-default-route: false  # boolean
                      enable-aigp: null  # boolean
                  ipv4-labeled-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  ipv6-labeled-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l3vpn-ipv4-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l3vpn-ipv6-unicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l3vpn-ipv4-multicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l3vpn-ipv6-multicast:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l2vpn-vpls:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  l2vpn-evpn:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  srte-policy-ipv4:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                  srte-policy-ipv6:
                    prefix-limit:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
                    prefix-limit-received:
                      config:
                        max-prefixes: null  # uint32
                        prevent-teardown: false  # boolean
                        warning-threshold-pct: null  # oc-types:percentage
              enable-bfd:
                config:
                  enabled: null  # boolean
                  desired-minimum-tx-interval: null  # uint32
                  required-minimum-receive: null  # uint32
                  detection-multiplier: null  # uint8
        ospfv2:
          global:
            config:
              router-id: null  # yang:dotted-quad
              summary-route-cost-mode: RFC2328_COMPATIBLE  # enumeration, enum: RFC1583_COMPATIBLE|RFC2328_COMPATIBLE
              igp-shortcuts: null  # boolean
              log-adjacency-changes: null  # boolean
              hide-transit-only-networks: null  # boolean
            timers:
              spf:
                config:
                  initial-delay: null  # uint32
                  maximum-delay: null  # uint32
              max-metric:
                config:
                  set: null  # boolean
                  timeout: null  # uint64
                  include: []  # leaf-list, identityref base oc-ospft:MAX_METRIC_INCLUDE
                  trigger: []  # leaf-list, identityref base oc-ospft:MAX_METRIC_TRIGGER
              lsa-generation:
                config:
                  initial-delay: null  # uint32
                  maximum-delay: null  # uint32
            graceful-restart:
              config:
                enabled: null  # boolean
                helper-only: null  # boolean
            mpls:
              config:
                traffic-engineering-extensions: null  # boolean
              igp-ldp-sync:
                config:
                  enabled: null  # boolean
                  post-session-up-delay: null  # uint32
            inter-area-propagation-policies:
              inter-area-propagation-policy:
              - src-area: null  # leafref
                dst-area: null  # leafref
                config:
                  src-area: null  # leafref
                  dst-area: null  # leafref
                  import-policy: []  # leaf-list, leafref
                  default-import-policy: REJECT_ROUTE  # default-policy-type
          areas:
            area:
            - identifier: null  # leafref
              config:
                identifier: null  # oc-ospf-types:ospf-area-identifier
              mpls:
                config:
                  traffic-engineering-enabled: null  # boolean
              interfaces:
                interface:
                - id: null  # leafref
                  config:
                    id: null  # oc-if:interface-id
                    network-type: null  # identityref base oc-ospf-types:OSPF_NETWORK_TYPE
                    priority: null  # uint8
                    multi-area-adjacency-primary: true  # boolean
                    authentication-type: null  # string
                    metric: null  # oc-ospf-types:ospf-metric
                    passive: null  # boolean
                    hide-network: null  # boolean
                  interface-ref:
                    config:
                      interface: null  # leafref
                      subinterface: null  # leafref
                  timers:
                    config:
                      dead-interval: null  # uint32
                      hello-interval: null  # uint32
                      retransmission-interval: null  # uint32
                  mpls:
                    config:
                      traffic-engineering-metric: null  # uint32
                    igp-ldp-sync:
                      config:
                        enabled: null  # boolean
                        post-session-up-delay: null  # uint32
                  lsa-filter:
                    config:
                      all: null  # boolean
                  neighbors:
                    neighbor:
                    - router-id: null  # leafref
                      config:
                        router-id: null  # yang:dotted-quad
                        metric: null  # oc-ospf-types:ospf-metric
                  enable-bfd:
                    config:
                      enabled: null  # boolean
                      desired-minimum-tx-interval: null  # uint32
                      required-minimum-receive: null  # uint32
                      detection-multiplier: null  # uint8
              virtual-links:
                virtual-link:
                - remote-router-id: null  # leafref
                  config:
                    remote-router-id: null  # inet:ipv4-address-no-zone
        ospfv3:
          global:
            config:
              router-id: null  # yang:dotted-quad
              log-adjacency-changes: null  # boolean
              hide-transit-only-networks: null  # boolean
              abr-capability: null  # identityref base oc-ospft:OSPF_ABR_TYPE
            timers:
              spf:
                config:
                  initial-delay: null  # uint32
                  maximum-delay: null  # uint32
              max-metric:
                config:
                  set: null  # boolean
                  timeout: null  # uint64
                  include: []  # leaf-list, identityref base oc-ospft:MAX_METRIC_INCLUDE
                  trigger: []  # leaf-list, identityref base oc-ospft:MAX_METRIC_TRIGGER
              lsa-generation:
                config:
                  initial-delay: null  # uint32
                  maximum-delay: null  # uint32
            graceful-restart:
              config:
                enabled: null  # boolean
                helper-only: null  # boolean
            inter-area-propagation-policies:
              inter-area-propagation-policy:
              - src-area: null  # leafref
                dst-area: null  # leafref
                config:
                  src-area: null  # leafref
                  dst-area: null  # leafref
                  import-policy: []  # leaf-list, leafref
                  default-import-policy: REJECT_ROUTE  # default-policy-type
          areas:
            area:
            - identifier: null  # leafref
              config:
                identifier: null  # oc-ospf-types:ospf-area-identifier
                type: oc-ospf-types:NORMAL  # identityref base oc-ospf-types:OSPF_AREA_TYPE
                stub-default-cost: null  # uint32
                import-summaries: true  # boolean
              interfaces:
                interface:
                - id: null  # leafref
                  config:
                    id: null  # string
                    network-type: null  # identityref base oc-ospf-types:OSPF_NETWORK_TYPE
                    priority: 1  # uint8
                    multi-area-adjacency-primary: true  # boolean
                    authentication-type: null  # string
                    metric: 10  # oc-ospf-types:ospf-metric
                    passive: false  # boolean
                    hide-network: null  # boolean
                  interface-ref:
                    config:
                      interface: null  # leafref
                      subinterface: null  # leafref
                  timers:
                    config:
                      dead-interval: 40  # uint32
                      hello-interval: 10  # uint32
                      retransmission-interval: 5  # uint32
                      interface-transmission-delay: 1  # uint32
                  lsa-filter:
                    config:
                      all: null  # boolean
                  static-neighbors:
                    static-neighbor:
                    - neighbor-ip-address: null  # leafref
                      config:
                        neighbor-ip-address: null  # inet:ip-address
                        metric: null  # oc-ospf-types:ospf-metric
                        poll-interval: null  # uint16
                        priority: 1  # uint8
                  enable-bfd:
                    config:
                      enabled: null  # boolean
                      desired-minimum-tx-interval: null  # uint32
                      required-minimum-receive: null  # uint32
                      detection-multiplier: null  # uint8
              virtual-links:
                virtual-link:
                - remote-router-id: null  # leafref
                  config:
                    remote-router-id: null  # inet:ipv4-address-no-zone
              address-ranges:
                address-range:
                - prefix: null  # leafref
                  config:
                    prefix: null  # inet:ip-prefix
                    status: ADVERTISE  # enumeration, enum: ADVERTISE|DONOTADVERTISE
        isis:
          global:
            config:
              authentication-check: true  # boolean
              instance: 0  # string
              instance-id: null  # uint16
              net: []  # leaf-list, oc-isis-types:net
              maximum-area-addresses: 3  # uint8
              level-capability: LEVEL_1_2  # oc-isis-types:level-type
              max-ecmp-paths: null  # uint8
              weighted-ecmp: false  # boolean
              poi-tlv: false  # boolean
              iid-tlv: false  # boolean
              fast-flooding: true  # boolean
              csnp-enable-on-p2p-links: true  # boolean
              hello-padding: STRICT  # oc-isis-types:hello-padding-type
            lsp-bit:
              overload-bit:
                config:
                  set-bit: false  # boolean
                  set-bit-on-boot: false  # boolean
                  advertise-high-metric: false  # boolean
                reset-triggers:
                  reset-trigger:
                  - reset-trigger: null  # leafref
                    config:
                      reset-trigger: null  # identityref base oc-isis-types:OVERLOAD_RESET_TRIGGER_TYPE
                      delay: null  # uint16
              attached-bit:
                config:
                  ignore-bit: false  # boolean
                  suppress-bit: false  # boolean
            reference-bandwidth:
              config:
                reference-bandwidth: null  # uint32
            nsr:
              config:
                enabled: false  # boolean
            graceful-restart:
              config:
                enabled: false  # boolean
                helper-only: null  # boolean
                non-planned-only: null  # boolean
                restart-time: 30  # uint16
                interface-timer: null  # uint16
                interface-time-expirations: null  # int64
            timers:
              config:
                lsp-lifetime-interval: 1200  # uint16
                lsp-refresh-interval: null  # uint16
              spf:
                config:
                  spf-hold-interval: 5000  # uint64
                  spf-first-interval: null  # uint64
                  spf-second-interval: null  # uint64
              lsp-generation:
                config:
                  lsp-max-wait-interval: null  # uint64
                  lsp-first-wait-interval: null  # uint64
                  lsp-second-wait-interval: null  # uint64
            transport:
              config:
                lsp-mtu-size: null  # uint16
            mpls:
              igp-ldp-sync:
                config:
                  enabled: true  # boolean
                  post-session-up-delay: null  # uint16
            igp-shortcuts:
              afi:
              - afi-name: null  # leafref
                config:
                  afi-name: null  # identityref base oc-isis-types:AFI_TYPE
                  nh-type: []  # leaf-list, identityref base oc-mplst:PATH_SETUP_PROTOCOL
            afi-safi:
              af:
              - afi-name: null  # leafref
                safi-name: null  # leafref
                config:
                  afi-name: null  # identityref base oc-isis-types:AFI_TYPE
                  safi-name: null  # identityref base oc-isis-types:SAFI_TYPE
                  metric: 10  # uint32
                  enabled: null  # boolean
                  max-ecmp-paths: null  # uint8
                  suppress-interface-reachability: NONE  # oc-isis-types:suppress-interface-ip-mode
                multi-topology:
                  config:
                    afi-name: null  # identityref base oc-isis-types:AFI_TYPE
                    safi-name: null  # identityref base oc-isis-types:SAFI_TYPE
            segment-routing:
              config:
                enabled: null  # boolean
                srgb: null  # leafref
                srlb: null  # leafref
              flex-algorithm-bindings:
                flex-algorithm-binding:
                - flex-algo-id: null  # leafref
                  config:
                    flex-algo-id: null  # uint8
                    isis-level: LEVEL_1_2  # oc-isis-types:level-type
                    advertised: false  # boolean
                    participate: false  # boolean
            inter-level-propagation-policies:
              level1-to-level2:
                config:
                  import-policy: []  # leaf-list, leafref
                  default-import-policy: REJECT_ROUTE  # default-policy-type
              level2-to-level1:
                config:
                  import-policy: []  # leaf-list, leafref
                  default-import-policy: REJECT_ROUTE  # default-policy-type
          levels:
            level:
            - level-number: null  # leafref
              config:
                enabled: false  # boolean
                level-number: null  # oc-isis-types:level-number
                metric-style: null  # oc-isis-types:metric-style
                authentication-check: true  # boolean
              graceful-restart:
                config:
                  enabled: false  # boolean
                  restart-time: 30  # uint16
              system-level-counters:
              traffic-engineering:
                config:
                  enabled: false  # boolean
                  ipv4-router-id: null  # oc-inet:ipv4-address
                  ipv6-router-id: null  # oc-inet:ipv6-address
              route-preference:
                config:
                  external-route-preference: null  # uint32
                  internal-route-preference: null  # uint32
              authentication:
                config:
                  enabled: false  # boolean
                  disable-csnp: false  # boolean
                  disable-psnp: false  # boolean
                  disable-lsp: false  # boolean
                  keychain: null  # oc-keychain:keychain-ref
                  auth-type: null  # identityref base oc-keychain-types:AUTH_TYPE
                  auth-mode: null  # identityref base oc-isis-types:AUTH_MODE
                  auth-password: null  # oc-types:routing-password
              afi-safi:
                af:
                - afi-name: null  # leafref
                  safi-name: null  # leafref
                  config:
                    afi-name: null  # identityref base oc-isis-types:AFI_TYPE
                    safi-name: null  # identityref base oc-isis-types:SAFI_TYPE
                    suppress-interface-reachability: null  # oc-isis-types:suppress-interface-ip-mode
          interfaces:
            interface:
            - interface-id: null  # leafref
              config:
                enabled: false  # boolean
                interface-id: null  # oc-if:interface-id
                passive: false  # boolean
                hello-padding: null  # oc-isis-types:hello-padding-type
                circuit-type: null  # oc-isis-types:circuit-type
              circuit-counters:
              authentication:
                config:
                  enabled: false  # boolean
                  keychain: null  # oc-keychain:keychain-ref
                  auth-type: null  # identityref base oc-keychain-types:AUTH_TYPE
                  auth-mode: null  # identityref base oc-isis-types:AUTH_MODE
                  auth-password: null  # oc-types:routing-password
              afi-safi:
                af:
                - afi-name: null  # leafref
                  safi-name: null  # leafref
                  config:
                    afi-name: null  # identityref base oc-isis-types:AFI_TYPE
                    safi-name: null  # identityref base oc-isis-types:SAFI_TYPE
                    enabled: null  # boolean
              levels:
                level:
                - level-number: null  # leafref
                  config:
                    level-number: null  # oc-isis-types:level-number
                    passive: false  # boolean
                    priority: null  # uint8
                    enabled: false  # boolean
                  packet-counters:
                    lsp:
                    iih:
                    ish:
                    esh:
                    psnp:
                    csnp:
                    unknown:
                  timers:
                    config:
                      hello-interval: null  # uint32
                      hello-multiplier: null  # uint8
                  afi-safi:
                    af:
                    - afi-name: null  # leafref
                      safi-name: null  # leafref
                      config:
                        afi-name: null  # identityref base oc-isis-types:AFI_TYPE
                        safi-name: null  # identityref base oc-isis-types:SAFI_TYPE
                        metric: 10  # uint32
                        enabled: null  # boolean
                        suppress-reachability: null  # boolean
                      segment-routing:
                        prefix-sids:
                          prefix-sid:
                          - prefix: null  # leafref
                            config:
                              prefix: null  # inet:ip-prefix
                              sid-id: null  # oc-srt:sr-sid-type
                              label-options: null  # enumeration, enum: NO_PHP|EXPLICIT_NULL
                        flex-algo-prefix-sids:
                          flex-algo-prefix-sid:
                          - prefix: null  # leafref
                            flex-algo-id: null  # leafref
                            config:
                              prefix: null  # inet:ip-prefix
                              flex-algo-id: null  # uint8
                              sid-id: null  # oc-srt:sr-sid-type
                        adjacency-sids:
                          adjacency-sid:
                          - sid-id: null  # leafref
                            neighbor: null  # leafref
                            config:
                              sid-id: null  # union
                              protection-eligible: true  # boolean
                              group: false  # boolean
                              neighbor: null  # inet:ip-address
                  hello-authentication:
                    config:
                      enabled: false  # boolean
                      keychain: null  # oc-keychain:keychain-ref
                      auth-type: null  # identityref base oc-keychain-types:AUTH_TYPE
                      auth-mode: null  # identityref base oc-isis-types:AUTH_MODE
                      auth-password: null  # oc-types:routing-password
              timers:
                config:
                  csnp-interval: null  # uint16
                  lsp-pacing-interval: null  # uint64
              weighted-ecmp:
                config:
                  load-balancing-weight: auto  # union
              enable-bfd:
                config:
                  enabled: null  # boolean
                  desired-minimum-tx-interval: null  # uint32
                  required-minimum-receive: null  # uint32
                  detection-multiplier: null  # uint8
              bfd:
                config:
                  bfd-tlv: null  # boolean
              graceful-restart:
                config:
                  interface-timer: null  # uint16
                  interface-time-expirations: null  # int64
              mpls:
                igp-ldp-sync:
                  config:
                    enabled: true  # boolean
                    post-session-up-delay: null  # uint16
              interface-ref:
                config:
                  interface: null  # leafref
                  subinterface: null  # leafref
        pim:
          global:
            config:
              maximum-groups: null  # uint32
            ssm:
              config:
                ssm-ranges: null  # leafref
            rendezvous-points:
              rendezvous-point:
              - address: null  # leafref
                config:
                  address: null  # inet:ipv4-address
                  multicast-groups: null  # string
          interfaces:
            interface:
            - interface-id: null  # leafref
              config:
                enabled: false  # boolean
                interface-id: null  # oc-if:interface-id
                mode: null  # identityref base oc-pim-types:PIM_MODE
                bsr-border: false  # boolean
                border-router: false  # boolean
                dr-priority: null  # oc-pim-types:dr-priority-type
                join-prune-interval: null  # oc-pim-types:pim-interval-type
                hello-interval: null  # oc-pim-types:pim-interval-type
                dead-timer: null  # uint16
                maximum-groups: null  # uint32
              interface-ref:
                config:
                  interface: null  # leafref
                  subinterface: null  # leafref
              enable-bfd:
                config:
                  enabled: null  # boolean
                  desired-minimum-tx-interval: null  # uint32
                  required-minimum-receive: null  # uint32
                  detection-multiplier: null  # uint8
        igmp:
          global:
            ssm:
              mappings:
                mapping:
                - source: null  # leafref
                  config:
                    source: null  # inet:ipv4-address
                    ssm-ranges: null  # leafref
          interfaces:
            interface:
            - interface-id: null  # leafref
              config:
                interface-id: null  # oc-if:interface-id
                enabled: false  # boolean
                version: null  # oc-igmp-types:igmp-version
                query-interval: null  # oc-igmp-types:igmp-interval-type
                filter-prefixes: null  # string
              counters:
                queries:
                  sent:
                  received:
                reports:
              membership-groups:
              static-membership-groups:
                static-groups:
                - static-group: null  # leafref
                  config:
                    static-group: null  # inet:ipv4-address
                    source: null  # inet:ipv4-address
              interface-ref:
                config:
                  interface: null  # leafref
                  subinterface: null  # leafref
        pcep:
          path-computation-servers:
            path-computation-server:
            - pce-server-address: null  # leafref
              config:
                pce-server-address: null  # oc-inet:ip-address
                id: null  # string
                port: 4189  # oc-inet:port-number
                source-address: null  # oc-inet:ip-address
                pce-type: null  # pce-mode-type
                preference: null  # uint8
                report-local-lsp: false  # boolean
                pce-initiated-capability: false  # boolean
                sr-support: false  # boolean
              timers:
                config:
                  keepalive: 30  # uint8
                  dead-timer: 120  # uint8
                  redelegation-timeout-interval: null  # uint16
                  state-timeout-interval: null  # uint16
              authentication:
                config:
                  enable: false  # boolean
                  authentication-key: null  # oc-types:routing-password
                  keychain: null  # oc-keychain:keychain-ref
"openconfig-platform:components":
  component:
  - name: null  # leafref
    config:
      name: null  # string
    properties:
      property:
      - name: null  # leafref
        config:
          name: null  # string
          value: null  # union
    subcomponents:
      subcomponent:
      - name: null  # leafref
        config:
          name: null  # leafref
    chassis:
      config:
      utilization:
        resources:
          resource:
          - name: null  # leafref
            config:
              name: null  # string
              used-threshold-upper: null  # oc-types:percentage
              used-threshold-upper-clear: null  # oc-types:percentage
    port:
      config:
    power-supply:
      config:
    fan:
      config:
    fabric:
      config:
    storage:
      config:
    cpu:
      config:
    integrated-circuit:
      config:
      utilization:
        resources:
          resource:
          - name: null  # leafref
            config:
              name: null  # string
              used-threshold-upper: null  # oc-types:percentage
              used-threshold-upper-clear: null  # oc-types:percentage
    backplane:
      config:
    software-module:
      config:
    controller-card:
      config:
    boot-loader:
      config:
"openconfig-qos:qos":
  config:
  interfaces:
    interface:
    - interface-id: null  # leafref
      config:
        interface-id: null  # string
      interface-ref:
        config:
          interface: null  # leafref
          subinterface: null  # leafref
      input:
        config:
          buffer-allocation-profile: null  # leafref
          multicast-buffer-allocation-profile: null  # leafref
          unicast-buffer-allocation-profile: null  # leafref
        classifiers:
          classifier:
          - type: null  # leafref
            config:
              name: null  # leafref
              type: null  # enumeration, enum: IPV4|IPV6|MPLS|IPV4_MULTICAST|IPV6_MULTICAST
            terms:
        queues:
          queue:
          - name: null  # leafref
            config:
              name: null  # string
              queue-management-profile: null  # leafref
        scheduler-policy:
          config:
            name: null  # leafref
        virtual-output-queues:
          voq-interface:
          - name: null  # leafref
            config:
              name: null  # string
            queues:
              queue:
              - name: null  # leafref
                config:
                  name: null  # string
      output:
        config:
          buffer-allocation-profile: null  # leafref
          multicast-buffer-allocation-profile: null  # leafref
          unicast-buffer-allocation-profile: null  # leafref
        classifiers:
          classifier:
          - type: null  # leafref
            config:
              name: null  # leafref
              type: null  # enumeration, enum: IPV4|IPV6|MPLS|IPV4_MULTICAST|IPV6_MULTICAST
            terms:
        queues:
          queue:
          - name: null  # leafref
            config:
              name: null  # string
              queue-management-profile: null  # leafref
        scheduler-policy:
          config:
            name: null  # leafref
  classifiers:
    classifier:
    - name: null  # leafref
      config:
        name: null  # string
        type: null  # enumeration, enum: IPV4|IPV6|MPLS|ETHERNET
      terms:
        term:
        - id: null  # leafref
          config:
            id: null  # string
          conditions:
            l2:
              config:
                source-mac: null  # oc-yang:mac-address
                source-mac-mask: null  # oc-yang:mac-address
                destination-mac: null  # oc-yang:mac-address
                destination-mac-mask: null  # oc-yang:mac-address
                ethertype: null  # oc-pkt-match-types:ethertype-type
            ipv4:
              config:
                source-address: null  # oc-inet:ipv4-prefix
                source-address-prefix-set: null  # leafref
                destination-address: null  # oc-inet:ipv4-prefix
                destination-address-prefix-set: null  # leafref
                fragment-offsets: []  # leaf-list, oc-pkt-match-types:fragment-offset-range
                dscp: null  # oc-inet:dscp
                dscp-set: []  # leaf-list, oc-inet:dscp
                length: null  # uint16
                protocol: null  # oc-pkt-match-types:ip-protocol-type
                hop-limit: null  # uint8
              icmpv4:
                config:
                  type: null  # identityref base oc-icmpv4-types:TYPE
                  code: null  # identityref base oc-icmpv4-types:CODE
            ipv6:
              config:
                source-address: null  # oc-inet:ipv6-prefix
                source-address-prefix-set: null  # leafref
                source-flow-label: null  # oc-inet:ipv6-flow-label
                destination-address: null  # oc-inet:ipv6-prefix
                destination-address-prefix-set: null  # leafref
                destination-flow-label: null  # oc-inet:ipv6-flow-label
                flow-label: null  # oc-inet:ipv6-flow-label
                dscp: null  # oc-inet:dscp
                dscp-set: []  # leaf-list, oc-inet:dscp
                length: null  # uint16
                protocol: null  # oc-pkt-match-types:ip-protocol-type
                hop-limit: null  # uint8
              icmpv6:
                config:
                  type: null  # identityref base oc-icmpv6-types:TYPE
                  code: null  # identityref base oc-icmpv6-types:CODE
            transport:
              config:
                source-port: null  # oc-pkt-match-types:port-num-range
                source-port-set: null  # leafref
                destination-port: null  # oc-pkt-match-types:port-num-range
                destination-port-set: null  # leafref
                detail-mode: null  # enumeration, enum: EXPLICIT|BUILTIN
                explicit-detail-match-mode: null  # enumeration, enum: ANY|ALL
                explicit-tcp-flags: []  # leaf-list, identityref base oc-pkt-match-types:TCP_FLAGS
                builtin-detail: null  # enumeration, enum: TCP_INITIAL|TCP_ESTABLISHED|FRAGMENT
            mpls:
              config:
                traffic-class: null  # oc-mpls:mpls-tc
                start-label-value: null  # oc-mpls:mpls-label
                end-label-value: null  # oc-mpls:mpls-label
                ttl-value: null  # uint8
            fabric:
              config:
                fabric-priority: null  # uint8
          actions:
            config:
              target-group: null  # leafref
            remark:
              config:
                set-dscp: null  # uint8
                set-dot1p: null  # uint8
                set-mpls-tc: null  # uint8
  forwarding-groups:
    forwarding-group:
    - name: null  # leafref
      config:
        name: null  # string
        fabric-priority: null  # uint8
        output-queue: null  # leafref
        unicast-output-queue: null  # leafref
        multicast-output-queue: null  # leafref
  queues:
    queue:
    - name: null  # leafref
      config:
        name: null  # string
        queue-id: null  # uint8
  scheduler-policies:
    scheduler-policy:
    - name: null  # leafref
      config:
        name: null  # string
      schedulers:
        scheduler:
        - sequence: null  # leafref
          config:
            sequence: null  # uint32
            type: null  # identityref base oc-qos-types:QOS_SCHEDULER_TYPE
            priority: null  # enumeration, enum: STRICT
          inputs:
            input:
            - id: null  # leafref
              config:
                id: null  # string
                input-type: null  # enumeration, enum: QUEUE|IN_PROFILE|OUT_PROFILE
                queue: null  # leafref
                weight: null  # uint64
          output:
            config:
              output-type: null  # enumeration, enum: SCHEDULER|FWD_GROUP|INTERFACE
              child-scheduler: null  # leafref
              output-fwd-group: null  # leafref
          one-rate-two-color:
            config:
              cir: null  # uint64
              cir-pct: null  # oc-types:percentage
              cir-pct-remaining: null  # oc-types:percentage
              bc: null  # uint32
              queuing-behavior: null  # oc-qos-types:queue-behavior
              max-queue-depth-bytes: null  # uint32
              max-queue-depth-packets: null  # uint32
              max-queue-depth-percent: null  # oc-types:percentage
            conform-action:
              config:
                set-dscp: null  # uint8
                set-dot1p: null  # uint8
                set-mpls-tc: null  # uint8
            exceed-action:
              config:
                set-dscp: null  # uint8
                set-dot1p: null  # uint8
                set-mpls-tc: null  # uint8
                drop: null  # boolean
          two-rate-three-color:
            config:
              cir: null  # uint64
              cir-pct: null  # oc-types:percentage
              cir-pct-remaining: null  # oc-types:percentage
              pir: null  # uint64
              pir-pct: null  # oc-types:percentage
              pir-pct-remaining: null  # oc-types:percentage
              bc: null  # uint32
              be: null  # uint32
            conform-action:
              config:
                set-dscp: null  # uint8
                set-dot1p: null  # uint8
                set-mpls-tc: null  # uint8
            exceed-action:
              config:
                set-dscp: null  # uint8
                set-dot1p: null  # uint8
                set-mpls-tc: null  # uint8
                drop: null  # boolean
            violate-action:
              config:
                set-dscp: null  # uint8
                set-dot1p: null  # uint8
                set-mpls-tc: null  # uint8
                drop: null  # boolean
  buffer-allocation-profiles:
    buffer-allocation-profile:
    - name: null  # leafref
      config:
        name: null  # string
      queues:
        queue:
        - name: null  # leafref
          config:
            name: null  # leafref
            dedicated-buffer: null  # uint64
            dedicated-buffer-temporal: null  # uint64
            use-shared-buffer: null  # boolean
            shared-buffer-limit-type: null  # identityref base SHARED_BUFFER_LIMIT_TYPE
            static-shared-buffer-limit: null  # uint32
            static-shared-buffer-limit-temporal: null  # uint32
            dynamic-limit-scaling-factor: null  # int32
            trim-enable: null  # boolean
  queue-management-profiles:
    queue-management-profile:
    - name: null  # leafref
      config:
        name: null  # string
      wred:
        uniform:
          config:
            min-threshold: null  # uint64
            max-threshold: null  # uint64
            min-threshold-percent: null  # uint64
            max-threshold-percent: null  # uint64
            enable-ecn: false  # boolean
            drop: false  # boolean
            weight: null  # uint32
            max-drop-probability-percent: null  # oc-types:percentage
      red:
        uniform:
          config:
            min-threshold: null  # uint64
            max-threshold: null  # uint64
            min-threshold-percent: null  # uint64
            max-threshold-percent: null  # uint64
            enable-ecn: false  # boolean
            drop: false  # boolean
  packet-trim:
    config:
      symmetric-dscp: null  # uint8
      fabric-priority: null  # uint8
"openconfig-routing-policy:routing-policy":
  defined-sets:
    prefix-sets:
      prefix-set:
      - name: null  # leafref
        config:
          name: null  # string
          mode: null  # enumeration, enum: IPV4|IPV6|MIXED
        prefixes:
          prefix:
          - ip-prefix: null  # leafref
            masklength-range: null  # leafref
            config:
              ip-prefix: null  # oc-inet:ip-prefix, mandatory
              masklength-range: null  # string
    neighbor-sets:
      neighbor-set:
      - name: null  # leafref
        config:
          name: null  # string
          address: []  # leaf-list, oc-inet:ip-address
    tag-sets:
      tag-set:
      - name: null  # leafref
        config:
          name: null  # string
          tag-value: []  # leaf-list, oc-pol-types:tag-type
  policy-definitions:
    policy-definition:
    - name: null  # leafref
      config:
        name: null  # string
      statements:
        statement:
        - name: null  # leafref
          config:
            name: null  # string
          conditions:
            config:
              call-policy: null  # leafref
              install-protocol-eq: null  # identityref base oc-pol-types:INSTALL_PROTOCOL_TYPE
            match-interface:
              config:
                interface: null  # leafref
                subinterface: null  # leafref
            match-prefix-set:
              config:
                prefix-set: null  # leafref
                match-set-options: null  # oc-pol-types:match-set-options-restricted-type
            match-neighbor-set:
              config:
                neighbor-set: null  # leafref
                match-set-options: null  # oc-pol-types:match-set-options-restricted-type
            match-tag-set:
              config:
                tag-set: null  # leafref
                match-set-options: null  # oc-pol-types:match-set-options-restricted-type
          actions:
            config:
              policy-result: null  # policy-result-type
            set-tag:
              config:
                mode: null  # enumeration, enum: INLINE|REFERENCE
              inline:
                config:
                  tag: []  # leaf-list, oc-pol-types:tag-type
              reference:
                config:
                  tag-set: null  # leafref
